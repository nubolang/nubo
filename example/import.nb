import App from "./app";

event click(id: string, data: int);

struct User {
    // name: ref string?
    ctx: nubo.Context
    others: dict[nbu.Ctx, dict[int.class, dict[string, int]]]
    list: [][]dict[int, int]
    handler: fn(int, string, dict[string,int]) -> fn() -> dict[string, int]
}
