import { Portal } from "@std/thread"

let portal = Portal(10)
defer portal.close()

spawn fn() void {
    portal.send(concat("Finish ", string(__concurrent__)))
}()

println(portal.receive())
println(__concurrent__)
